<% if @chore.user == current_user %>
<h1>Edit Chores</h1>
<%= link_to "Forget it -- I did it already", chore_path(@chore), method: :delete, data: { confirm: "Are you sure you don't want someone to do this?" } %>
<%= render 'form' %>

<% else %>
	<div style="margin-top: 300px; border: 2px solid black; text-align: center; font-size: 40px; color: red; ">FORBIDDEN ACCESS</div>
<% end %>